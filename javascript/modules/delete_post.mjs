import { removePost } from "../api/posts/delete.mjs";

export function deletePostListener() {
	const container = document.querySelector(".fa-trash-can");
	console.log(container);

	//addEventListener("click", function () {
	//	removePosts();
	//});
}
